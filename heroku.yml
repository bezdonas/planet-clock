setup:
  config:
    NODE_ENV: production
build:
  docker:
    web: Dockerfile
  config:
    NODE_ENV: production
release:
  command:
    - npm run build
  image: web
run:
  web:
    command: 
      - npm install -g serve
      - serve -s build
    image: web