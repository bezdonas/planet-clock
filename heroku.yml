setup:
  config:
    NODE_ENV: production
build:
  docker:
    web: Dockerfile
    post: 
      - npm run build
      - npm install -g serve
  config:
    NODE_ENV: production
release:
run:
  web:
    command: 
      - serve -s build
    image: web