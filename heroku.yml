setup:
  config:
    NODE_ENV: production
build:
  docker:
    web: Dockerfile
  config:
    NODE_ENV: production
  post: 
    - npm run build
release:
  command:
    - npm install -g serve
  image: web
run:
  web:
    command:
      -serve -s build
    image: web