--- 
build: 
  config: 
    NODE_ENV: production
  docker: 
    post: 
      - "npm run build"
      - "npm install -g serve"
    web: Dockerfile
run: 
  web: 
    command: 
      - "serve -s build"
    image: web
setup: 
  config: 
    NODE_ENV: production
