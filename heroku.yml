setup:
  config:
    NODE_ENV: production
build:
  docker:
    web: Dockerfile
  config:
    NODE_ENV: production
release:
  command:
    - npm run build
    - npm install -g serve
  image: web
run:
  web:
    - serve -s build